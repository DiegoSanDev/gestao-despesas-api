spring:
  application:
    name: gestao-despesas-api
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:dbGestaoDespesas
    username: sa
    password: sa
  sql:
    init:
      mode: always
springdoc:
  .swagger-ui:
    .path:
      - /swagger-ui.html
      - /swagger-ui/*
